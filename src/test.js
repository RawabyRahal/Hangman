// import React, { useState } from 'react';
// function MyComponent() {
//   const [myState, setMyState] = useState(initialValue);
//   const handleClick = () => {
//     // Using the callback form of setState
//     setMyState((prevValue) => {
//       // prevValue is the previous state
//       console.log('Previous State:', prevValue);
//       // Perform any action with the previous state
//       const newValue = calculateNewValue(prevValue);
//       // Return the new state
//       return newValue;
//     });
//     // This console log might not reflect the updated state
//     // because setMyState is asynchronous
//     console.log('State immediately after setMyState:', myState);
//   };
//   return (
//     <div>
//       <p>Current State: {myState}</p>
//       <button onClick={handleClick}>Update State</button>
//     </div>
//   );
// }











